# maps/full/ —— 完整地图（只用于显示）

识别命中后，悬浮窗里显示的就是这里对应的整张地图。尺寸任意。

文件名要和 `maps/door/` 里的门裁剪图**同名**，例如 `full/map1.jpg` ↔ `door/map1.jpg`。

这一层可以不放东西：如果 `maps/` 根目录下已经有同名的整图，程序会自动拿它来显示。

（本文件是 .md，不会被当成模板加载。）
