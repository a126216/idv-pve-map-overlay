# 2026-09-16

## MapMatcher 开源准备
- 评估结论：代码成熟度=高，可作为简历作品；开源前补齐 4 个 blocker。
- 已落地：LICENSE(MIT)、.gitignore(忽略 .venv/__pycache__/*.log/*.tmp/config.json/maps 图片素材，保留 maps/*.md)、config.example.json、probe.py 硬编码路径改为 tempfile.gettempdir()。
- README 增加「从仓库运行」与「地图素材与版权」两节。
- 已 `git init`（仅初始化，未提交）。注意：.gitignore 不支持行内注释，config.json 那行曾因尾部 `#` 注释失效，已改为独立注释行。
- 待用户自行：git remote add + commit + push（建议放链接前再补英文 README / PyInstaller spec / CI）。

## 第二轮：作品观感增强（已交付）
- 双语 README.md：中文保留 + 完整英文翻译；并补「测试与 CI」「打包为可执行文件」两节（中英）。
- 合成图冒烟测试 tests/test_smoke.py：用 numpy 生成结构化合成图，验证 SIFT 引擎 8/8 正确识别 + template(MAE) 模式自身 MAE≈0。不依赖游戏素材/显示器；template 测试在缺 PyQt5 时自动跳过。
- CI：.github/workflows/ci.yml，ubuntu + Python 3.11/3.12，仅装 numpy + opencv-python-headless 跑 tests/test_smoke.py。
- PyInstaller：MapMatcher.spec（onedir、console=False、收集 PyQt5/cv2 数据、按需打包本地 maps 与 icon.ico）。
- 验证方式：本地用项目 .venv（cv2 5.0.0/numpy 2.5.3）实测 SIFT 8/8、template 自身 MAE=0、tests 退出码 0；spec/测试文件 py_compile 通过。
- 经验：PyInstaller spec 用 collect_data_files/collect_dynamic_libs 收集 PyQt5 与 cv2；纯 SIFT 冒烟测试只需 opencv-python-headless 即可在 headless CI 跑，template 测试需 PyQt5 故在 CI 跳过。
